spring:
  application:
    name: category-service

---
spring:
  profiles:
    active: local
---
spring:
  profiles:
    active: kubernetes
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: event
      config:
        sources:
          - name: ${spring.application.name}
