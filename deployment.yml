apiVersion: apps/v1
kind: Deployment
metadata:
  name: category
  labels:
    app: category
spec:
  replicas: 1
  selector:
    matchLabels:
      app: category
  template:
    metadata:
      labels:
        app: category
    spec:
      containers:
        - name: category
          image: gcr.io/caliber-dev-254513/category@sha256:5c79d003e8e9a5f5952a4bd2e8239ad96e0c04b3e3a5e577b74452355bd09883 
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /actuator/health
              port: 80
              scheme: HTTP
            initialDelaySeconds: 60
            successThreshold: 1
          ports:
            - containerPort: 80
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /actuator/health
              port: 80
              scheme: HTTP
            initialDelaySeconds: 10
            successThreshold: 1
      serviceAccountName: config-reader

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: category
  name: category
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 10000
  selector:
    app: category
  type: LoadBalancer

---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: config-reader
  namespace: default

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: default
  name: pod-reader
rules:
  - apiGroups: [""]
    resources: ["pods","configmaps"]
    verbs: ["get", "watch", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-reader
  namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: pod-reader
subjects:
  - kind: ServiceAccount
    name: config-reader
    namespace: default

